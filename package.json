{
  "name": "juke-box",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && yarn start\"",
    "build": "cd client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod": "cd server && NODE_ENV=production node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^3.5.1"
  }
}
